from django.db import models


# Create your models here.
# class Owns(models.Model):
#     user = models.OneToOneField("auth.user", on_delete=models.CASCADE, primary_key=True, related_name='contact_owns')
#     contact_list = models.OneToOneField(ContactList, on_delete=models.CASCADE, related_name='owner')
