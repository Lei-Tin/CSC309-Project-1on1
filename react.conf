server {
    listen 80 default_server;
    server_name _;
    root /var/www/CSC309-Project-1on1/react/build;  # Change this path to the location of your build files
    index index.html index.htm;

    location / {
        try_files $uri /index.html;
    }
}